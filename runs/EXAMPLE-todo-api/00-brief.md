# Brief: todo-api

A small REST API for a personal todo app. Single user, no auth for v1. CRUD on
todos (title, done, created_at). Must persist to disk and be runnable locally
with one command. Goal: a clean reference implementation, not a product.
